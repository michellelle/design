<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <% if params[:plan] =='3' %>
      <h1>Pro Account</h1>
      <p>Design your workspace, collaborte, and commence virtuosity. $10 same access</p>
    <% else %>
      <h3>Design your workspace, collaborte, and commence virtuosity.</h3>
    <% end %>
  </div>
  <div class="col-md-6 col-md-offset-3 text-center">
    <h2>Sign up</h2>
      
      <% if params[:plan] == '3' %>
        <%= render "pro_form" %>
      <% else %>
        <%= render "basic_form" %>
      <% end %>
      
      <%= render "devise/shared/links" %>
  </div>
</div>